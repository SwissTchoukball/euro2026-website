name: Build and deploy

on:
  push:
    branches:
      - main

env:
  BUILD_DIR: .output

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@master

      - name: Setup Node.js 22
        uses: actions/setup-node@v4.4.0
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci

      - name: Build Nuxt app
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: nuxt-build
          if-no-files-found: error
          include-hidden-files: true
          path: ${{ env.BUILD_DIR }}

  deploy:
    name: Deploy to staging
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: Staging
      url: https://next.euro2026.ch

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: nuxt-build
          path: build

      - name: Start ssh-agent and add key
        uses: webfactory/ssh-agent@v0.5.4
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Ensure server SSH host key is known
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ vars.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Sync files to server
        run: |
          rsync -avz --delete \
            -e "ssh -o StrictHostKeyChecking=yes" \
            build/ ${{ vars.SERVER_USER }}@${{ vars.SERVER_HOST }}:/srv/customer/sites/next.euro2026.ch

      # We can't do that yet on Infomaniak. And they don't use pm2.
      # When Infomaniak will support programmatic restart, we should uncomment and adapt this step.
      # - name: Restart Nuxt service
      #   run: |
      #     ssh ${{ vars.SERVER_USER }}@${{ vars.SERVER_HOST }} \
      #       "cd /srv/customer/sites/next.euro2026.ch && pm2 reload nuxt-app"
